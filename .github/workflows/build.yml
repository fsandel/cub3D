name: Build

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: macos-11

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make
    - name: Clean up
      run: make fclean
