#include <cub3D.h>

uint8_t	get_pixel_from_texture(t_vector *target, t_map *map, int line_height,
			int y, t_direction direction)
{
	(void)target;
	(void)map;
	(void)line_height;
	(void)y;
	(void)direction;
	return (0);
}
